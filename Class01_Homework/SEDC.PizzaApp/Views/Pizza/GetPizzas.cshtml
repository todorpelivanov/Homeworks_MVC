@*the dat is sent to the view is of type List<Pizza *@
@model List<Pizza>

    <h2>Pizzas:</h2>
    <ul>
    @for(int i=0; i < @Model.Count; i++) {
        <li>@Model.ElementAt(i).Name</li>
    }

    </ul>